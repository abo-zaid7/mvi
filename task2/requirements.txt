# Task 2 runs on TensorFlow / Keras.  These versions are pinned because
# TensorFlow 2.13 needs numpy < 2, and installing anything newer silently
# breaks the whole stack.
tensorflow==2.13.0
tensorflow-metal==1.0.1   ; sys_platform == "darwin"   # Apple silicon GPU
numpy==1.24.3
scipy==1.11.4
scikit-learn==1.3.2
scikit-image==0.22.0
opencv-python==4.8.1.78
pandas==2.0.3
matplotlib==3.7.5
joblib
